# Adım 1: Kütüphaneler ve CSV yükleme

import pandas as pd
import numpy as np
import tensorflow as tf
from itertools import combinations

# Eğer Colab kullanıyorsan, dosya yükleme için:
from google.colab import files
import io

print("📂 Lütfen Süper Loto CSV dosyanızı yükleyin (Date, Num1~Num6 sütunları olmalı).")

uploaded = files.upload()
filename = next(iter(uploaded))

# CSV'yi yükle ve tarihi datetime yap, sayıları listeye al
df = pd.read_csv(io.StringIO(uploaded[filename].decode('utf-8')))
df['Date'] = pd.to_datetime(df['Date'])
df['Numbers'] = df[['Num1','Num2','Num3','Num4','Num5','Num6']].values.tolist()

print(f"✅ Veri yüklendi. Toplam çekiliş sayısı: {len(df)}")
print(df.head())
